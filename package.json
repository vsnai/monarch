{
  "name": "demo",
  "private": true,
  "version": "0.0.1",
  "main": "dist/my-element.es.js",
  "exports": {
    ".": "./dist/my-element.es.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:dev": "vite build --mode development",
    "preview": "vite build && vite preview --port 3000",
    "preview:dev": "vite build --mode development && vite preview --port 3000",
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "test:ui": "vitest --ui"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "date-fns": "^2.28.0",
    "express": "^4.18.1",
    "lit": "latest"
  },
  "devDependencies": {
    "@vitest/ui": "latest",
    "c8": "latest",
    "cross-fetch": "latest",
    "happy-dom": "latest",
    "msw": "latest",
    "vite": "latest",
    "vitest": "latest"
  },
  "msw": {
    "workerDirectory": "public"
  }
}
